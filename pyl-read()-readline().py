#line = open('myfile.txt','r')
#i = 0
#while True:
    #i = i + 1
    #line = f.readline()
    #if not line:
     #   break
    #m1 = line.split(",")[0]
   # m2 = line.split(",")[1]
   # m3 = line.split(",")[2]
    #print(f"Marks of student 1 in maths is:{i}")
    #print(f"Marks of student 2 in english is: {i}")
    #print(f"Marks of  student 3 in sst is: {i}")
    #if not line
   # print(line)
     # break
     
     f = open('myflie2.txt','w')
     lines = ['line 1\n', 'line 2\n', 'line 3\n']
     f.writeline(lines)
     f.close()