#def average(a=9, b= 1):
   # print("The average is", (a+b))
    
    #average(4, 6)
    #average(b = 9)
    
    # def name (fname, sname, fname = "payal");
 # print("Hello", fnmae, sname, fname)
 # name("Any" payal)
  
  #Keyword argument
  #def average(a=9, b= 1):
    #print("The average is", (a+b))
    
   # average(4, 6)
   # average(b=9) 
    #average (a = 21)
    
    #def average(*number):
        # print(type(number))
        #sum = 0 
        #for i in number:
            #sum = sum + i 
            #print("Anerage is", sum / len(number))
            
            
           # def name(**name):
    #print("hello,", name["fname"] name["lname"])
    
    #name(mname="Buchanan", lname="Barnes" fname"jamea")